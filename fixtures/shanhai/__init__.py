"""ShanHai text fixtures.

``generated/`` holds the UTF-8 text fixtures (byte-deterministic, LF, no BOM);
``truth/`` holds the normal fixture's truth manifest, which pins the exact
character span of every provision. Both are produced by ``build_text_fixtures.py``.
"""
